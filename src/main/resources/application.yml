server:
  port: 8080

spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      routes:
        # Auth Service?
        - id: auth-service
          uri: http://localhost:8081
          predicates:
            - Path=/api/v1/auth/**